# Synthetic notes project

This directory is intentionally fictional and contains no real records. It
exists only so the checked-in workspace manifest has a safe, deterministic
project route to validate.
